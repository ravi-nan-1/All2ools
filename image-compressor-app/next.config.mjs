/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Optional: Add a basePath if you are deploying to a sub-path of a domain
    // basePath: '/image-compressor-app',
};

export default nextConfig;
